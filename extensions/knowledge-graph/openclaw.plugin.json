{
  "id": "knowledge-graph",
  "name": "Knowledge Graph",
  "version": "0.2.0",
  "description": "Stores facts as (subject, relation, object) triples via the Knowledge Graph REST API (Memgraph-backed). Fact extraction is handled by the Darwin agent.",
  "entry": "src/index.ts",
  "configSchema": {
    "type": "object",
    "properties": {
      "apiBaseUrl": {
        "type": "string",
        "default": "http://localhost:8000",
        "description": "Base URL of the Knowledge Graph REST API (no trailing slash)"
      },
      "apiKey": {
        "type": "string",
        "description": "Bearer token for the API (leave blank if the API runs without authentication)"
      },
      "minConfidence": {
        "type": "number",
        "default": 0.6
      }
    },
    "required": ["apiBaseUrl"],
    "additionalProperties": false
  },
  "uiHints": {
    "apiBaseUrl": {
      "label": "API Base URL"
    },
    "apiKey": {
      "label": "API Key",
      "sensitive": true
    },
    "minConfidence": {
      "label": "Minimum Confidence"
    }
  }
}
