### **2026-02-21: Key Actions & Updates**

#### **Skills Setup & Configuration**

1. **Trading 212 Skill**
   - Full API client implemented for Trading 212 (Invest & Stocks ISA accounts)
   - Supports: account summary, positions, orders (market/limit/stop/stop-limit), historical data, dividends, transactions
   - Uses Basic Auth with API Key ID + Secret Key
   - CLI: `node /workspace/skills/trading212/dist/trading212.js <command>`
   - Example: `trading212 positions` → lists all open positions with P&L

2. **EventRegistry Skill**
   - Created TypeScript client for EventRegistry (NewsAPI.ai) API
   - Supports: article search, event search, trending concepts/categories, top headlines
   - Free plan limits: last 30 days only, 1 token/article search, 5 tokens/event search
   - CLI: `node /workspace/skills/eventregistry/dist/eventregistry.js <command>`
   - Example: `eventregistry headlines --count 5` → top 5 headlines

3. **gogcli Skill**
   - CLI tool for Google Workspace (Gmail, Calendar, Drive, Sheets, Docs, Contacts, Tasks, etc.)
   - Binary: `gog` (available in sandbox via bind mount from host `/usr/local/bin/gog`)
   - Example: `gog gmail search 'is:unread' --max 5` → latest unread emails
   - Auth: credentials/token in `~/.config/gogcli/` (bind-mounted from host); keyring unlocked via `GOG_KEYRING_PASSWORD` env var
   - Account: `luka@stopar.si`

4. **Microsoft 365 Skill**
   - Installed `microsoft365` skill from ClawHub
   - Needs device code auth (browser flow) — not fully configured yet
   - App registration requires: public client flow enabled, redirect URI `http://localhost`

5. **Tool & Model Updates**
   - **Models**: Switched to Claude Opus (`anthropic/claude-opus-4-6`) for this session
   - **Tools**: Confirmed all 15 core tools functional (file ops, exec, web fetch, messaging, memory)
   - **No `web_search` tool**: Not available in OpenClaw config; use EventRegistry/arXiv instead

---

#### **Credentials & Security Notes**

- **Trading 212**: Credentials stored in `/workspace/.trading212-config.json` (API Key ID + Secret Key)
- **EventRegistry**: API key in `/workspace/.eventregistry-config.json`
- **gogcli (Google Workspace)**: credentials/token in `~/.config/gogcli/` (bind-mounted); keyring via `GOG_KEYRING_PASSWORD` env var
- **Jira/Confluence**: Credentials via CLI config files in `/workspace/.config/`
- **Always send credentials in private chat** — never in group channels

---

#### **Open Issues**

1. **Microsoft 365 Auth**: Device code flow requires browser interaction (not sandbox-friendly)
   - Workaround: Run auth on host, copy tokens to workspace
2. **Web Search**: No built-in tool; use `web_fetch` (limited) or install a ClawHub skill
3. **Model Updates**: `devstral-2512` added to config but needs gateway restart to take effect

---

#### **Next Steps**

- Complete Microsoft 365 auth setup
- Explore web search skills on ClawHub
- Test `devstral-2512` after gateway restart