# Darwin Run — 2026-03-04 16:30 CET (ad-hoc / manual trigger)

## Sessions Scanned: 8

| Session | Notes |
|---|---|
| agent:main:telegram:group:-5247564907 | Main Telegram group — active |
| agent:lstopar-agent:main:thread:615 | NEW session — full history scanned (GLM-5 research, SSHFS, von Neumann timeout) |
| agent:lstopar-agent:main:thread:2320 | Previously scanned — checked for new msgs |
| agent:lstopar-agent:main | NEW — main agent session |
| agent:lstopar-agent:main:thread:701 | Trading212 balance checks (post-lastRunAt: only balance queries, no new stable facts) |
| agent:lstopar-agent:cron:68e53dae | Email Triage cron — empty (0 messages) |
| agent:darwin:telegram:group:-5247564907 | Darwin delivery channel — only cron result messages |
| agent:main:main | Agamemnon main — backup cron results |

## Facts Added: 19

### GLM-5 (new entity enrichment, 6 facts)
- `developed_by` → Z.ai (Zhipu AI)
- `architecture` → MoE (Mixture of Experts) with DeepSeek Sparse Attention
- `total_parameters` → 744B
- `active_parameters_per_token` → 40B
- `gguf_q4km_actual_size` → 456 GB (11 shards, ~41.5 GB/shard) ← initial estimate of 81 GB was 10× wrong
- `confluence_page` → https://edgetech-atlassian.atlassian.net/wiki/spaces/Engineerin/pages/20611074/GLM-5+infra

### topniska.stopar.si (new entity, 3 facts)
- `type` → remote Linux server
- `ssh_port` → 2244
- `owned_by` → Luka Stopar

### OpenClaw (1 new fact)
- `github_repo` → EdgeTechSolutions/openclaw.git (URL moved from previous)

### ATVP (new entity, 1 fact)
- `documents_in` → OpenClaw workspace (lstopar-agent), added 2026-03-04

### von Neumann (new entity, 2 facts)
- `is_type_of` → OpenClaw agent (software development specialist)
- `incident` → timed out on 2026-03-03/04 (reason unknown, session logs unavailable)

### Microsoft 365 skill (new entity, 3 facts)
- `installed_at` → /workspace/skills/microsoft365/ (lstopar-agent workspace)
- `interface_type` → CLI (node email-triage.js list|get|move|reply-draft) — not MCP tools
- `token_scopes` → Mail.ReadWrite (confirmed in token)

### Email Triage Agent (new entity, 2 facts)
- `cron_job_id` → 68e53dae-c120-44b5-82f9-55b5b4866473
- `fix_applied` → prompt rewritten to use CLI commands; delivery.mode=none (2026-02-26)

### Keynes (1 new fact added to existing entity)
- `confirmed_access_to` → Trading212 skill CLI

## Facts Skipped (duplicates): 3
- GLM-5 `is_type_of` LLM — already present
- OpenClaw `config_setting` (tools.sessions.visibility=all) — already present
- Keynes existing facts (is_type_of, spawned_by, uses_service) — already present

## Notes
- Semantic search (Gemini text-embedding-004 API) was returning HTTP 404 during this run — `add` actions succeeded (they don't require embeddings), but `search` actions failed. Logged; will resolve on its own if API recovers.
- Thread:615 was a new session not scanned before — full history from 2026-02-28 onward captured.
- Von Neumann timeout investigation: agent couldn't determine root cause (session logs had been cleaned up). Fact logged as incident with unknown cause.
- ATVP project appeared for the first time (from daily backup noting new ATVP documents). No additional context available yet.
